#!/usr/bin/env node
"use strict";

const { callTool } = require("../lib/mcp");

(async () => {
  try {
    const res = await callTool("who_am_i", {});
    console.log(JSON.stringify(res, null, 2));
  } catch (e) {
    console.error(`error: ${e.message}`);
    process.exit(2);
  }
})();
